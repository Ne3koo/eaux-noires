{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <main class="container">
        <div class="row">
            <section class="col-md-8">
                <h2>{{ category.name }}</h2>
                <div id="article-list" class="article-container">
                    {% include 'article/list.html.twig' with { articles: articles } %}
                </div>
            </section>
        </div>
    </main>
{% endblock %}
