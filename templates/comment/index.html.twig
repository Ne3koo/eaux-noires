{% set username = comment.user.username %}
<div class="comment" id="c_{{ comment.id }}">
    <div class="card">
        <div class="card-body">
        <h5 class="card-title">
            <a href="">
                <span class="comment-author">{{ username }}</span>
            </a> {{ comment.createdAt|date('d M Y à H:i:s') }}
        </h5>
        </div>
    </div>
</div>