{% for article in articles %}
    {% include 'article/item.html.twig' with { article } %}
{% endfor %}

<div class="container">
    <section class="">
        {{ knp_pagination_render(articles) }}
    </section>
</div>